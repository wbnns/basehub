---
title: "consensus/protocol"
description: "Base-specific protocol types — batches, channels, frames, and compression (brotli, zlib)."
---

import { LinkCard } from '@astrojs/starlight/components';

## Overview

The `consensus/protocol` sub-crate (`base-protocol`) is part of the [consensus crate group](/crates/consensus/overview/).

Base-specific protocol types — batches, channels, frames, and compression (brotli, zlib).

## Source

<LinkCard title="View source on GitHub" href="https://github.com/base/base/tree/main/crates/consensus/protocol" />
