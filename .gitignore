# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# transient upstream sync artifacts (snapshot.txt is built each run;
# pr-body.md is rendered each run; only last-sync.txt and diff.txt
# are committed by CI)
.upstream-state/snapshot.txt
.upstream-state/pr-body.md
